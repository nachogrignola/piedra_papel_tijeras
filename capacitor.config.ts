import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'piedra_papel_tijeras',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
